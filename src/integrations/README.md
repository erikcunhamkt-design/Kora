# src/integrations — Integrações externas

Clientes e tipos de serviços externos usados pelo frontend. `supabase/` contém o
client gerado (`client.ts`) e os tipos do banco (`types.ts`), consumidos por toda
a aplicação. **Arquivos gerados** — não editar `client.ts`/`types.ts` à mão.

# database — Banco de dados

Documentação de referência do banco: schema, políticas de RLS e migrações
explicadas. As migrações SQL versionadas ficam em
[`../../supabase/migrations`](../../supabase/migrations); o detalhamento de schema
por módulo está em [`../features`](../features).
